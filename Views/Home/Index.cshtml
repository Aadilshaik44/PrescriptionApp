@using PrescriptionApp.Models
@model List<Prescription>
@{
    ViewData["Title"] = "Home Page";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="mb-2">
    <a asp-controller="Prescription" asp-action="Add" class="btn btn-success">
        Add New Prescription</a>
</div>

<table class="table table-bordered table-striped mx-auto mt-3
" style="background-color: rgba(255, 255, 255, 0.9);">
    <thead >
        <tr>
            <th>Medication Name</th>
            <th>Fill Status</th>
            <th>Cost</th>
            <th>Request Date</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var prescription in Model)
        {
            <tr>
                <td>@prescription.MedicationName</td>
                <td>@prescription.FillStatus</td>
                <td>@prescription.Cost.ToString("C")</td>
                <td>@prescription.RequestTime.ToString("MM/dd/yyyy HH:mm")</td>
                <td>
                    <a asp-controller="Prescription" asp-action="Edit" asp-route-id="@prescription.PrescriptionId">Edit</a>
                    <a asp-controller="Prescription" asp-action="Delete" asp-route-id="@prescription.PrescriptionId">Delete</a>
                </td>
             </tr>
        }
    </tbody>
</table>